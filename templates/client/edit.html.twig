{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit Client'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'Edit Client'|trans }}</h1>

    {{ include('client/_form.html.twig', {'button_label': 'Update'}) }}

 
    <a href="{{ path('client_index') }}" class="btn btn-dark">{{ 'back to list'|trans }}</a>

    {{ include('client/_delete_form.html.twig') }}
{% endblock %}
